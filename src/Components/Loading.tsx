import { Html } from '@react-three/drei';

const Loading: React.FC = () => {
  return (
    <Html center className="font-bold text-white">
      Loading...
    </Html>
  );
};

export default Loading;
